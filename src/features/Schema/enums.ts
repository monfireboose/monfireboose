export enum QueryConditionEnum {
  equals = '==',
  notEquals = '!=',
  gt = '>',
  lt = '<',
  gte = '>=',
  lte = '<=',
  in = 'in',
  notIn = 'not-in',
  arrayContains = 'array-contains',
  arrayContainsAny = 'array-contains-any',
}
